<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <item
        android:id="@+id/main_graph"
        android:icon="@drawable/ic_warehouse"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/second_graph"
        android:icon="@drawable/ic_this_side_up"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/third_graph"
        android:icon="@drawable/ic_delivery_truck_24_hours"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/fourth_graph"
        android:icon="@drawable/ic_container"
        tools:ignore="MenuTitle" />

</menu>